from creameta.meta import ImWatchingYou

def gosub(foo):
    "runs an abstract subroutine command pattern instance"

class PythonClass(object):
    __metaclass__ = ImWatchingYou

    def __init__(self, **kwargs):
        self.__dict__.update(kwargs)

    def contains(self):
        for a in self:
            if a == None:
                return "null"

raw_input("On to the next, press Enter to continue...")

class AbstractEntityContainerAdapterFactory(object):
    __metaclass__ = ImWatchingYou

    def __init__(self, **kwargs):
        self.__dict__.update(kwargs)

    def contains(self):
        for a in self:
            if len(a) != 0:
                for b in a:
                    gosub(b)
                    for c in b:
                        for t in c:
                            for v in t:
                                for b in v:
                                    for n in b:
                                        for x in n:
                                            pass

